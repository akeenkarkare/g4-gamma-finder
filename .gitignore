# Build artifacts
build/

# Python virtual environment
ml_env/
__pycache__/
*.pyc

# Generated data / model outputs (regenerate via macros + training)
*.csv
save/models/
save/training/
runs/

# ...but keep the curated key result datasets (small, for reproducibility)
!datasets_keep/
!datasets_keep/*.csv

# OS cruft
.DS_Store
